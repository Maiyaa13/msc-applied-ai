# MSc Applied Artificial Intelligence

**Name:** Mahesh Janaranjana
**Index No:** 20260064
**Programme:** MSc in Applied Artificial Intelligence (IIT)

A portfolio of my MSc coursework: graded assignments and lecture notes. Each assignment folder
contains the executed Jupyter notebook and a rendered copy (PDF or HTML) with all outputs
visible.

## Assignments

| Folder | Assignment | Summary |
|--------|-----------|---------|
| `a04_lda_qda/` | Logistic Regression, LDA and QDA | Compare LDA vs QDA on the sklearn digits dataset, covariance analysis, decision boundaries, and a logistic-regression baseline. |
| `a06_cnn/` | Convolutional Neural Networks | LeNet-5 trained on MNIST, controlled experiments, and ResNet18 transfer learning on CIFAR-10 (two stages), in PyTorch. |
| `a08_spacy/` | RegEx and NLP with spaCy | Regex phone-number extraction, spaCy token attributes, rule-based Matcher, NER analysis, and a hybrid regex + dependency-parsing function. |
| `a09_transformers_mlflow/` | Transformers and MLflow | Sentiment classification, NER and text generation with Hugging Face pipelines, each tracked in MLflow with multiple runs and a systematic comparison. |

## Lecture notes

See `lecture_notes/` (added over the course of the programme).

## Environment

Work was done in Python with PyTorch, scikit-learn, spaCy, Hugging Face Transformers and
MLflow. The notebooks are self-contained; large model caches, datasets and virtual
environments are intentionally excluded from the repository.
